<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>
    <PropertyGroup>
        <!-- AOT enabled with source-generated JSON serialization -->
        <PublishTrimmed>true</PublishTrimmed>
        <PublishAot>true</PublishAot>
        <!-- Single-file executable that doesn't require .NET runtime -->
        <PublishSingleFile>true</PublishSingleFile>
        <SelfContained>true</SelfContained>
        <!-- Enable compression for smaller executables -->
        <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Extism.runtime.all" />
        <PackageReference Include="Extism.Sdk" />
        <PackageReference Include="System.CommandLine" />
        <PackageReference Include="WolverineFx" />
        <PackageReference Include="FluentValidation" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Morphir.Tooling\Morphir.Tooling.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Ionide.KeepAChangelog.Tasks" PrivateAssets="all" />
    </ItemGroup>

</Project>
