namespace Morphir.Models.Tests.IR.DSL

open Expecto

module ValuesTests =

    [<Tests>]
    let tests =
        testList "DSL Values" [
            // TODO: Re-enable these tests after Values DSL design review
            // The Values DSL hasn't been through the design review process yet
        ]

